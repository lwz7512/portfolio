doctype
html(lang=config.language)
    head
        include head
    body
        .wrap
            header
                .title-bar
                  = config.title
                  button(type="button").ltmenu
                    //- | MENU
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.logo-link(href=url_for())
                    img(src=url_for(theme.logo) alt="logo")
                include nav
            main.container
                block container
            footer
                block pagination
                block copyright
        include scripts
